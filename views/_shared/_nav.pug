mixin nav()
    ul
        li
            a(href="/")
                | Home
        li
            a(href="/posts")
                | All posts
        li
            a(href="/posts/form")
                | Add post
        li
            a(href="/posts/categories")
                | All Post Categories
        if user
            p
                =user.username  
            form(action="/auth/sign-out" method="post")
                button.nav-btn
                    | sign-out
            if user.uuid
                img(src="/static/uploads/" + user.uuid + "/file/" + user.filename, alt="").img-thumbnail
            
        else
            button.nav-btn
                a(href="/auth/sign-in") Sign In               