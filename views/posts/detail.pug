extends ../_shared/_master
block body
    each post in context
        h1
            =post.title
        p
            =post.text
        p
            =post.category
        p
            =post.date
        p
            | posted by: 
            =post.user
        img(src="/static/uploads/" + post.uuid + "/file/" + post.filename, alt="")